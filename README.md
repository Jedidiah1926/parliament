# Hemicycle

> 레트로 CRT 터미널 스타일의 의회 시뮬레이터 — 정당·연정·의석 구성, 입법·표결, 선거를 브라우저에서 직접 운영합니다.

---

## 개요

Hemicycle(구 DATANET Parliament Simulation)은 설치나 빌드 없이 **브라우저에서 바로 실행**되는 의회 시뮬레이션 도구입니다.

- 단원제 / 양원제 / 삼원제 자유 전환
- 이념 스펙트럼에 따른 정당·연정 구성, 무소속 개별 의원 관리
- 법안 작성 → 상정 → 표결 → 개정안까지 이어지는 입법 절차
- 지역구·비례대표를 혼합할 수 있는 선거 시뮬레이션과 기록 보관
- 전체 상태를 `.json`으로 저장/불러오기

한국어 화면(`dno.html`)을 기준으로 개발되며, 설정에서 영어를 선택하면 `lang.js`가 화면에 표시되는 텍스트를 실시간으로 영어로 치환해 보여줍니다 — 별도의 영어 전용 HTML/JS 사본을 유지하지 않습니다.

---

## 실행 방법

| 파일 | 설명 |
|------|------|
| `index.html` | 점검 시간대 자동 판정 후 `main.html`로 리디렉션 |
| `main.html` | 시작 메뉴 (버전 정보, 로드맵 링크, 시뮬레이터 실행) |
| `dno.html` | 시뮬레이터 본체 |
| `settings.html` | 설정 화면 (언어 선택) |
| `roadmap.html` | 개발 로드맵 및 업데이트 내역 |
| `teaser.html` | 티저 페이지 (구 `dno.pre.html`) |
| `lang.js` | 언어 설정 저장 및 한국어→영어 실시간 치환 엔진 (`dno.html`/`roadmap.html`에서 사용) |

`index.html` 또는 `dno.html`을 브라우저에서 직접 열면 됩니다. Chrome, Firefox, Edge 최신 버전에서 동작하며, 한글 글꼴(네오둥근모 · Pretendard)은 `fonts/` 폴더에 함께 들어 있어 인터넷 없이도 보이고, 영문 보조 글꼴 VT323만 Google Fonts에서 불러옵니다.

---

## 데스크톱 앱 (Windows .exe) 빌드

Electron으로 감싸 설치형 Windows 앱(.exe)으로도 빌드할 수 있습니다. 게임 내부 로직·화면은 웹 버전과 완전히 동일하며, `electron/main.js`가 `main.html`을 창으로 띄우는 껍데기만 추가된 구조입니다.

```bash
npm install          # Electron / electron-builder 설치 (최초 1회)
npm start             # 개발 중 바로 실행해보기
npm run build:win     # Windows 설치형 exe 빌드 → dist/Hemicycle-Setup-<버전>.exe
```

- Windows에서 빌드하는 게 가장 확실하지만, `electron-builder`는 Wine이 설치된 macOS/Linux에서도 Windows 타깃 빌드를 지원합니다.
- 빌드 설정은 `package.json`의 `build` 항목(electron-builder)에서 관리하며, 앱 이름·버전·설치 마법사 옵션(바탕화면/시작메뉴 바로가기 등)을 그곳에서 조정합니다.
- 데스크톱 앱은 로컬 파일(`localStorage`)에 세이브를 저장하므로 브라우저 버전과 데이터가 공유되지 않습니다.
- 예전 이름(DATANET Parliament Simulation)으로 설치했던 경우: Hemicycle은 별도의 앱으로 설치되며, 처음 실행할 때 예전 앱의 세이브·설정을 새 데이터 폴더(`%APPDATA%\Hemicycle`)로 자동으로 옮겨 옵니다. 옮겨진 걸 확인한 뒤 예전 앱은 제거해도 됩니다 (예전 데이터 폴더는 지워지지 않고 남습니다).

---

## 화면 구성

화면은 좌측 **컨트롤 패널**과 우측 **의석 시각화 패널**로 나뉩니다.

- 좌측 상단에서 **의회 형태**(단원제 / 양원제 / 삼원제)를 고르면 전체 UI가 그에 맞춰 재구성됩니다.
- `집권 세력 강조` 체크박스: 켜면 집권 세력(정당 또는 연정) 소속 의석에 금색 글로우 테두리가 표시됩니다.
- `저장 시 사진 포함` 체크박스: 당수·연정 대표 사진까지 저장 파일에 포함할지 선택합니다.
- 하단의 **`>> PROTOCOL EXECUTE <<`** 버튼으로 언제든 강제로 다시 그리고 통계를 갱신할 수 있습니다.
- 컨트롤 패널은 **정당 / 의회 / 입법 / 선거**, 네 개의 메인 탭으로 구성됩니다.

---

## 🏛 정당 탭

| 하위 탭 | 기능 |
|---|---|
| **이념** | 좌우 스펙트럼 상의 이념을 추가·삭제·드래그로 순서 변경. 정당은 기본적으로 이 순서를 따라 자동 정렬됩니다. |
| **정당** | 정당 이름·색상·로고·설명 편집(내부 탭 "정당"), 각 정당에 **파벌**을 추가해 파벌별 이름·색상·의석 배분 가능. 당수 이름·사진 편집(내부 탭 "당수"). |
| **연정** | 각 연정의 대표 이름·사진을 편집(의석 배정 자체는 "의회 > 연정" 탭에서 진행). |
| **무소속** | 하원/상원/삼원별로 무소속 의석 각각에 이름·사진·이념을 개별 지정. |

---

## ⚙ 의회 탭

| 하위 탭 | 기능 |
|---|---|
| **설정** | 의원실(하원/상원/삼원) 이름과 총 의석 수 지정, 정당별 이름·색상·의석 수·이념 설정. ▲▼ 버튼으로 정당 순서 수동 변경 가능하며, 수동 정렬 중에는 `↺ 자동정렬` 버튼으로 이념 순 복귀. 하원·상원·삼원 참여 여부는 의원실별로 독립 설정됩니다. |
| **연정** | 여러 정당을 묶어 연정을 구성(체크박스로 멤버 지정), **각외협력**(정식 참여 없이 신임·예산 등에서만 정부를 지지하는 정당) 명칭과 대상 지정, 대표당·집권 세력 지정. "무소속" 탭에서 개별 배정된 무소속 의원은 이름(또는 좌석번호)으로 별도 표시됩니다. |
| **의원** | 지역구 선거로 당선된 의원 개인의 이름·궐석 여부 등을 관리합니다. |
| **무소속** | 개별 무소속 의원을 특정 연정의 **정식 멤버** 또는 **각외협력**으로 배정합니다(소속 없음도 선택 가능). |

집권 세력 의석은 캔버스에서 금색 실선, 각외협력 의석은 금색 점선으로 강조 표시됩니다.

---

## ⚖ 입법 탭

| 하위 탭 | 기능 |
|---|---|
| **제출** | 법안 제목·내용·태그를 작성하고 가결 기준(과반 / 특별다수 2·3 / 전원일치 / 직접 지정 n분의 m)을 설정해 등록. 등록된 법안은 "기존 법안 수정"에서 상정 전까지 다시 고칠 수 있습니다. |
| **상정** | 대기 중인 법안 목록을 검색·태그 필터로 조회하고, 표결에 올릴 법안을 선택합니다. |
| **표결** | 심의할 법안을 고른 뒤 투표 입력 모드(찬성/반대/기권/제거 또는 좌석 정보 보기)를 선택해 캔버스에서 좌석을 클릭하거나, 정당 일괄 투표로 한 정당 전체에 한 번에 적용합니다. 하원 표결을 확정해야 상원이, 상원(또는 하원)을 확정해야 삼원이 열리며, 앞 단계가 부결되면 다음 단계는 자동으로 **미상정** 처리됩니다. |
| **기록** | 가결·부결이 완료된 법안 아카이브(최신순). 가결은 초록, 부결은 빨간 테두리로 표시되며, "▾ 세부 기록" 토글을 열면 의원실별 표결 결과가 실제 표결 화면과 동일한 막대그래프(가결선 포함)로 재현됩니다. **가결된 법안은 "📝 개정안 발의" 버튼으로 개정안을 작성할 수 있고**, 개정안에는 원본 대비 버전 번호(제n판)가 부여되며 원본 법안 카드에서 딸린 개정안 목록도 함께 확인할 수 있습니다. |

### 양원제 표결 흐름

```
법안 제출
    │
    ▼
하원 표결 ──── 부결 ──▶ 상원 미상정 → 최종 부결
    │
   가결
    │
    ▼
상원 표결 ──── 부결 ──▶ 최종 부결
    │
   가결
    │
    ▼
  최종 가결
```

삼원제에서는 상원(또는 양원제가 아니면 하원) 가결 뒤 삼원 표결이 이어지며, 같은 규칙으로 앞 단계 부결 시 이후 단계는 미상정 처리됩니다.

---

## 🗳 선거 탭

| 하위 탭 | 기능 |
|---|---|
| **지역구** | 헥스 그리드 위에 지역구를 그리거나 지우고, 이름을 지정합니다. 하원/상원/삼원별로 독립적으로 구성할 수 있습니다. |
| **성향** | 지역구별 특정 정당 지지 성향(가중치)을 칠해 지역 편차를 반영합니다. |
| **선거** | 정당별 지지율(%)과 오차 범위를 입력하고, 비례대표 / 지역구 / 혼합 방식 중 선택해 개표를 실행합니다. 무당파 지지층은 매 선거마다 무작위 비율로 정당에 배분되어 결과에 변동을 줍니다. 보궐선거(궐석 지역구만 재선거)도 지원합니다. |
| **기록** | 개표가 끝나면 자동으로 기록이 저장됩니다. 각 기록은 정당별 의석 요약과 함께 "▾ 세부 기록" 토글로 정당별 실제 득표율(개표에 실제로 반영된 가중치 기준)과 저장 시각을 확인할 수 있고, `↻ 의회 반영`으로 해당 결과를 의회에 즉시 적용하거나 `👁 보기`로 결과만 다시 열람할 수 있습니다. |

---

## 🖥 캔버스 조작

- 반원형(또는 지역구 지도) 배치도 위에 정당 색상의 점(좌석)이 표시됩니다. 점 테두리는 정당/연정/집권 세력 색, 점 내부는 표결 시 투표 상태 색입니다.
- **좌석에 마우스를 올리면** 흰색 고리가 좌석과 살짝 떨어진 위치에 나타나 클릭 가능한 좌석임을 표시합니다(토성 고리 스타일).
- **좌석을 클릭하면**
  - 입법 탭의 "표결" 화면이면서 투표 입력 모드(찬성/반대/기권/제거)가 선택되어 있을 때 → 해당 좌석에 투표가 입력됩니다. 같은 모드로 다시 클릭하면 초기화됩니다.
  - 그 외의 모든 경우(다른 탭에 있을 때, 또는 표결 화면에서 "🔍 정보 보기" 모드일 때) → **좌석 정보 카드**가 클릭 위치 근처에 뜹니다. 무소속이면 이름(또는 `#좌석번호`), 소속 정당이 있으면 "좌석 정보 #n"이 제목으로 표시되고, 집권 세력이면 제목 앞에 ★가 붙습니다. 본문에는 정당·파벌(있는 경우)·이념·연정·투표 상태가 표시됩니다.
- 정보 카드와 툴팁은 화면 오른쪽/아래 끝에서도 잘리지 않도록 항상 화면 안쪽으로 위치가 보정됩니다.
- 창 크기를 바꾸면 캔버스가 자동으로 다시 그려집니다.

---

## 저장 / 불러오기

화면 맨 위에는 항상 **세이브 탭 바**가 떠 있어, 브라우저 탭처럼 클릭 한 번으로 세이브 사이를 오갈 수 있습니다. 각 탭은 그 세이브 전용 자동저장을 따로 가지고 있어 다른 탭으로 넘어가도 진행 상황이 섞이지 않습니다.

`+`를 누르면 두 가지 중 고를 수 있습니다.
- **새로 생성**: 지금 화면 상태를 그대로 새 탭(세이브)으로 등록
- **프리셋에서 생성**: 미리 만들어둔 시나리오(프리셋) 목록에서 제목을 골라, 그 상태로 시작하는 새 탭을 만듦

### 프리셋 추가하는 법

프리셋은 시작 화면(세이브 선택)의 왼쪽 목록과, 게임 안 "+" 탭의 "프리셋에서 생성" 목록에 표시됩니다. 프리셋을 고르면 그 상태의 **복사본이 새 세이브로** 만들어지므로 원본 프리셋은 바뀌지 않습니다.

`presets/` 폴더의 `index.json`이 배포자가 추가하는 프리셋 목록을 관리합니다. 새 프리셋을 추가하려면:

1. 게임에서 원하는 시나리오 상태를 만든 뒤, 국가 > 설정 > 저장 탭의 **SAVE**(파일로 저장) 버튼으로 `.json` 파일을 하나 받습니다.
2. 그 파일을 `presets/` 폴더 안에 넣습니다 (예: `presets/founding_1948.json`).
3. `presets/index.json`에 항목을 하나 추가합니다.

```json
[
  { "file": "founding_1948.json", "title": "건국 초기 (1948)", "description": "선택 — 목록에 함께 표시되는 설명" }
]
```

`title`이 목록에 그대로 표시되고, `"tutorial": true`를 넣으면 그 프리셋으로 시작할 때 조작법 튜토리얼이 함께 켜집니다. 튜토리얼은 #1 ~ #5의 짧은 과정으로 나뉘어 있고, 표시된 곳을 직접 조작해야(정당 추가·의석 입력·법안 등록 등) 다음 단계로 넘어갑니다. 단계 내용은 `js/tutorial.js`의 `LESSONS`에 있습니다.

> `index.json` 방식은 `fetch`를 쓰기 때문에 웹(http)으로 열었을 때만 동작합니다. 데스크톱 앱(Electron)은 파일(`file://`)로 열려 JSON을 읽을 수 없으므로, 앱과 함께 기본 제공되는 **내장 프리셋**은 `presets/*.js`로 감싸 두고 `js/presets.js`의 `BUILTIN` 목록에 등록합니다 (예: 튜토리얼 공화국 — `presets/tutorial.js`).

**SAVE** 버튼을 누르면 현재 상태 전체를 `.json` 파일로 저장합니다.

저장 항목:
- 단원제·양원제·삼원제 설정 및 각 의원실 이름·총 의석 수
- 이념·정당(파벌 포함)·연정(각외협력·개별 무소속 배정 포함) 전체
- 정당 정렬 모드(자동/수동)
- 법안 전체, 버전/개정 관계, 의원실별 세부 표결 기록
- 지역구·성향 데이터와 선거 기록
- 현재 투표 입력 상태와 열려 있던 탭 위치

**LOAD** 버튼으로 저장 파일을 불러옵니다. 한국어 버전과 영어 버전은 저장 파일이 완전히 호환됩니다. 구버전 저장 파일(v1~v11)도 하위 호환으로 불러올 수 있으며, 이후 버전에 새로 추가된 값은 기본값으로 채워집니다.

---

## 🌐 번역(언어 팩) 만들기

화면은 한국어로 만들어져 있고, 다른 언어는 **언어 팩**(한국어 원문 → 번역 사전)으로 표시합니다. 언어 팩은 코드 없이 데이터만 담은 `.json` 파일이라 누구나 새 언어를 만들어 공유할 수 있습니다.

1. 메인 메뉴 › 설정의 **번역 템플릿 받기**로 `dno-lang-template.json`을 받습니다 (영어 번역이 채워진 상태).
2. `code`(예: `"ja"`, `"zh-tw"`)와 `name`(예: `"日本語"`)을 채우고, `dict`의 각 항목 `["한국어 원문", "번역"]`에서 **두 번째 칸만** 바꿉니다.
3. 설정의 **언어 팩 불러오기 (.json)**로 파일을 고르면 언어 목록에 추가되고 바로 쓸 수 있습니다 (브라우저에 저장, ✕로 삭제).

```json
{
  "format": "dno-lang-pack@1",
  "code": "ja",
  "name": "日本語",
  "author": "만든 사람",
  "months": ["1月", "2月", "3月", "4月", "5月", "6月", "7月", "8月", "9月", "10月", "11月", "12月"],
  "ordinal": "none",
  "patterns": [
    { "re": "(\\d+|\\?)년\\s*(\\d+|\\?)월\\s*(\\d+|\\?)일", "to": "$1年{{month:$2}}$3日" },
    { "re": "(\\d+)석", "to": "$1議席" }
  ],
  "dict": [
    ["⚙ 의회", "⚙ 議会"],
    ["[+] 정당 추가", "[+] 政党追加"]
  ]
}
```

- `dict`: 화면에 보이는 한국어 문구 → 번역. 긴 문구가 먼저 매칭되므로 문장 전체와 단어를 함께 넣어도 됩니다. `{ "원문": "번역" }` 객체로 써도 됩니다.
- `patterns`: 숫자가 섞인 문구(날짜·의석 수 등)를 위한 정규식 규칙. `to`에서 `$1`은 괄호 그룹, `{{month:$2}}`는 `months`의 달 이름, `{{ordinal:$1}}`은 서수(`"ordinal": "en"`이면 1st/2nd…, `"none"`이면 숫자 그대로), `{{map:$4|정기회=Regular Session;임시회=Extraordinary Session}}`는 값 바꾸기, `{{dict:$1}}`은 사전 찾기입니다.
- `code`는 `kr`(기본 한국어)와 기본 제공 언어(`en`)를 쓸 수 없습니다 — 기본 영어를 고쳐 쓰려면 `en-custom`처럼 다른 코드를 쓰세요.
- 기본 제공 언어는 `lang/<code>.js`에 같은 형식의 JSON을 JS로 감싸 두고 `js/lang.js`의 `BUILTIN`에 등록합니다 (데스크톱 앱이 `file://`로 열려 JSON 파일을 직접 읽을 수 없기 때문).

## 디자인

1970~80년대 군사·정부용 데이터 터미널의 시각 언어에서 영감을 받았습니다. 스캔라인 오버레이, CRT 글로우 효과, 고정폭 픽셀 폰트, 네온 시안 포인트 컬러가 특징입니다.

사용 폰트:
- **네오둥근모 (NeoDunggeunmo)** — 한글 픽셀 폰트 (SIL OFL 1.1, `fonts/neodgm/`에 함께 배포)
- **Pretendard** — 라이트/다크 테마 글꼴 (SIL OFL 1.1, `fonts/pretendard/`에 함께 배포)
- **VT323** — 영문 모노스페이스 폰트 (Google Fonts)

---

## 개발 현황

진행 중인 기능과 변경 이력은 `roadmap.html`(로드맵 및 업데이트 내역)에서 버전별로 확인할 수 있습니다.

---

## 라이선스

개인 및 교육 목적으로 제작되었습니다.
